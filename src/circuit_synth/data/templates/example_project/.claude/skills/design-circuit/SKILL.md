---
name: design-circuit
description: Iterative circuit design loop (write circuit-synth Python, generate KiCad schematic, simulate, refine). Placeholder — full workflow lands in stage 5.
---

Run `uv run python circuit-synth/main.py` to generate the example schematic.

(This is a stage-3 placeholder. The full read → think → write → generate →
simulate → examine → refine workflow is installed in stage 5.)
